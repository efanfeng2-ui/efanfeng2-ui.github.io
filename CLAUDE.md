# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

`AGENTS.md` (imported above) is the authoritative entry point for this site: what makes it different
from the upstream al-folio starter repo, the toolchain and its pinned versions, the validated command
set, and the content rules.

[`WEBSITE_PLAN.md`](WEBSITE_PLAN.md) is the build plan — read it before writing site content. It
carries the positioning strategy, per-page specs with English copy drafts, the phased execution plan,
and §7's list of facts the site owner still needs to verify before launch.
